const _= require('lodash')
// Gerar um valor aleatorio entre 1 e 1000 de 2 em 2 segundos
setInterval(() => console.log(_.random(1, 100)), 2000)

// Instalado dependencia lodash
// Instalado dependencia nodemon -> Live server do node